# Prisma 7 - Prisma PostgreSQL Setup with Node.js project -


## Step 1 - 

- Create Project

```
mkdir hello-prisma
cd hello-prisma
```

- initialize a Typescript Project

```
npm init
npm install typescript tsx @types/node --save-dev
npx tsc --init
```

- Install the packages needed for this quickstart:

```
npm install prisma @types/pg --save-dev
npm install @prisma/client @prisma/adapter-pg pg dotenv
```

- check tsconfig.json for this project and update it in your project accordingly (or you can copy past it)

- package.json

```typescript
{
  "type": "module"
}
```

- Initialize Prisma ORM

```
npx prisma init
```

- update prisma.config.ts

```typescript
// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import "dotenv/config";
import { defineConfig } from "prisma/config";


const databaseUrl = process.env["DATABASE_URL"];

if (!databaseUrl) {
  throw new Error("DATABASE_URL is not set in .env");
}


export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  datasource: {
    url: databaseUrl,
  },
});
```


## Step 2 -

- changed output in prisma/schema.prisma
- PrismaClient is in src/index.ts for this project you can make a seperate file for it in src/lib/prismaClient.ts 
- intalled tsup (npm install tsup) and added scripts in package.json

```
npm install tsup
```

- scripts in package.json

```typescript
"scripts": {
  "dev": "tsx src/index.ts",
  "build": "tsup src/index.ts --format esm --out-dir dist",
  "prod": "node dist/index.js"
}
```

